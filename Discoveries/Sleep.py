# Sleep is similar to setTimeout in JS. The difference is it cannot be inside of a function/def
# and instead of miliseconds/ms, it has to be in seconds.

import time

print ('Wait for it...')

time.sleep(5)
print ('Done.')